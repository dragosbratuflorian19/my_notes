Git is a version control system for tracking changes in files
Revert changes
Local and remote repos
touch app.py  # Creates a file

$ git init  # Initialize local Git Repository
$ git add < file >  # Add Files to Index
$ git add .  # Add everything
$ git rm - -cached < file >  # Remove a file
$ git status  # Check status of the working tree
$ git commit  # Check changes in Index
$ git commit - m 'Comentariu aici'  # Without editing
$ git push  # Push the local repository to the remote repository
$ git remote add origin <https://github.com/dragosbratuflorian19/Projects.git>
$ git push - u origin master
$ git push --set-upstream origin master # Commit first time
$ git pull  # Pull the latest changes from the remote repository
$ git clone  # Copy a remote repository into a new Directory
$ touch .gitignore
$ touch log.txt
$ in .gitignore write "log.txt"
$ git branch < o_ramura >  # Create a new branch
$ git checkout < o_ramura >  # Switch to the other branch
$ git merge < alta_ramura >  # Merge 2 branches
README.md  # Marked down
# Myapp
This is my app  # Readme file
$ git clone https: // github.com / dragosbratuflorian19 / MyAppSample.git  # Clone a repo
escape: wq to escape from edit
$ git config --global user.email 'dragosbratuflorian19@gmail.com'
$ git config --global user.name 'dragosbratuflorian19'
$ git add . != git reset
