Make a directory in terminal:								$ mkdir New_Folder
create a  file in a destination:							$ mkdir home / dragos / Downloads / new_Folder
create multiple directories:								$ mkdir f1 f2 f3	MKDIR
Remove a directory in terminal:								$ rmdir New_Folder	RMDIR
Change into a directory in the terminal:					$ cd New_Folder
go back:													$ cd ..
go to previous:												$ cd -
go to the root directory:									$ cd / CD
List files in directory:									$ ls
show all the files in subdirectories:						$ ls - R
ls - l:														$ Checks the permissions	LS
Print working directory:									$ pwd	PWD
copy a file in the same directory:							$ cp test.txt test_copy.txt
copy:														$ cp test.txt home / dragos / Downloads	CP
remove:														$ rm test_copy.txt
remove a folder with a file inside:							$ rm - r FOLDER
delete a file:												$ rm file1	RM
create a file:												$ cat > file1 'text...' CTRL + D
concatenate a file:											$ cat >> file1 'text...' CTRL + D
view the file:												$ cat file1	CAT
move a file:												$ mv file1 home / dragos / Downloads
rename a file:												$ mv file1 file2	MV
Checks the connection:										$ curl - I - s https: // opensource.com	CURL
tail - f:													$ see content of a file in real time
tail - n 100:												$ see last 100 lines	TAIL
processes:													$ ps
toate procesele:											$ ps - A
cate o pagina:												$ ps - A | more	PS
show the network status:									$ netstat - tulpn
Network connections and status of sockts:					$ netstat - r	NETSTAT
list of opened files:										$ lsof - i tcp: 80	LSOF
free space:													$ df - h 	DF
How much space uses some files:								$ du - h	DU
To give the execution right:								$ chmod + x fisier.py
To give the execution right:								$ chmod 761 fisier.py	CHMOD
Show history:												$ history	HISTORY
To not retype:												$ !19 !
Check the internet connectivity:							$ arp	ARP
Find a file in a directory:									$ find ex1.txt	FiND
to check network interface:									$ ifconfig	IFCONFIG
Restart:													$ init 5	INIT
all users currently on the server:							$ w	W
Making shortcuts:											$ alias	ALIAS
Locating a file:											$ locate fisier.txt	LOCATE
write to a file:											$ echo Manchester >> fisier	ECHO
Unzip a file:												$ tar xvzf < filename > -C / location/
Add a new environment variable:								$ nano ~ / .bashrc

